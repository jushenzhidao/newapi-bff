fastapi==0.115.*
uvicorn[standard]==0.30.*
httpx==0.27.*

# 会话 Cookie 的 AES-256-GCM 加解密（app/security.py）。
# 已移除 itsdangerous：它只签名不加密，载荷是 base64 明文 JSON，而会话里装着
# 用户的 new-api PAT —— 任何读到 Cookie 字符串的人无需密钥即可直接解出。
# 钉死小版本：加密是安全边界，不接受 * 通配带来的意外升级。
cryptography==43.0.3

# 可观测性（app/observability.py）。未配 LOGFIRE_TOKEN 时不导入 SDK、零副作用，
# 因此它对本地开发和 CI 不构成前置依赖。
# extras 带入 FastAPI / httpx 的 OTel instrumentation，缺了会导致埋点静默失效。
logfire[fastapi,httpx]==4.41.0
